# ImageProcessingServer

Компонент сервера для обработки полученных с автономного модуля изображений  

## Dependencies
Cmake >= 3.6  
OpenCV == 4.9.0
Натренированная модель Yolov8s.onnx

## Building

`mkdir build && cd build`  
`cmake ..`  
`make`
